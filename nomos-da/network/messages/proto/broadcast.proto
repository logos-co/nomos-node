syntax = "proto3";

package nomos.da.v1.broadcast;

import "common.proto";

// BROADCAST

message BroadcastReq {
    common.Blob blob = 1;
}

// WRAPPER MESSAGE

message Message {
    oneof message_type {
        BroadcastReq broadcast_req = 1;
        common.SessionReq session_req = 2;
    }
}
