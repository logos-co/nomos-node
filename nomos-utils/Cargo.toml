[package]
edition = "2021"
license = { workspace = true }
name    = "nomos-utils"
version = "0.1.0"

[features]
serde = ["dep:serde"]

[dependencies]
const-hex   = "1"
rand        = "0.8"
rand_chacha = "0.3"
serde       = { version = "1.0", optional = true }
